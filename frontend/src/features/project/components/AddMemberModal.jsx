import Modal from "../../../components/ui/Modal"

export default function AddMemberModal({ isOpen, onClose}) {
    return (
        <Modal
            isOpen={isOpen}
            onClose={onClose}
            title="Add Member"
        >

        </Modal>
    )
}